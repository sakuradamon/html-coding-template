doctype html
html(lang='ja')
    head
        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, user-scalable=no')
        block title
            title Default title
        meta(name='description', content='')
        meta(name='keywords', keywords='')
        meta(name="robots", content="noindex,nofollow,noarchive")
        link(href='https://fonts.googleapis.com/css?family=Quicksand:400,700', rel='stylesheet', media='all')
        link(href=root_dir+'css/style.css', rel='stylesheet', media='all')

    body
        header
            .c-container
                h1.logo Sample
                nav
                    ul
                        li: a.nav-link(href="#about") About
                        li: a.nav-link(href="#sample") Sample

        main
            block content

        footer
            .c-container
                p Copyright

        #page-top.c-page-top-button

        script(src=root_dir+'js/jquery.min.js')
        block script
